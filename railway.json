{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python knowledge_base/ingest.py && uvicorn api.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
